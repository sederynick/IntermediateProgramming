package generics.wildcard;

import java.util.Random;

public class TextFileEncryptDecrypt {
	private int key;
	
	public TextFileEncryptDecrypt(){
		Random rand = new Random(System.currentTimeMillis());
		key=rand.nextInt(100-6)+6;
	}
	public 

}
